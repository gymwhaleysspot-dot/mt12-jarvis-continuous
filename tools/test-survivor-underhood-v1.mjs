import fs from'node:fs';import vm from'node:vm';
const source=fs.readFileSync(new URL('../jarvis/survivor-underhood-v1.js',import.meta.url),'utf8'),context={console,Date,Math,Number,Set,Object};context.globalThis=context;vm.runInNewContext(source,context,{filename:'survivor-underhood-v1.js'});const e=context.JarvisSurvivorUnderhood;if(!e)throw Error('engine did not boot');if(e.updates.length!==200)throw Error('expected 200 updates');if(new Set(e.updates.map(x=>x.id)).size!==200)throw Error('update IDs are not unique');if(e.domains.length!==20)throw Error('expected 20 domains');if(!e.domains.every(d=>e.updates.filter(x=>x.domain===d.id).length===10))throw Error('domain coverage is not 10');e.boot({runId:'certification-test',level:1});for(let i=0;i<180;i++)e.tick({tick:i+1,level:1,frameMs:16.667,hero:{name:'GRIFFIN',x:.3,z:.6,hp:780},boss:{name:'LIRA',x:.7,z:.6,hp:900}});const result=e.certify({levelsCleared:45,terminalReplayCount:1,remoteRequired:true,remoteVerified:true});if(!result.passed)throw Error(JSON.stringify(result));const snapshot=e.snapshot();if(snapshot.ticks!==180||snapshot.checks<1||snapshot.violations.length)throw Error(JSON.stringify(snapshot));console.log(JSON.stringify({result:'PASS',updates:e.updates.length,domains:e.domains.length,ticks:snapshot.ticks,checks:snapshot.checks,certification:result.checks}));
