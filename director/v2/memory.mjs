import fs from'node:fs';
const isStr=x=>typeof x==='string'&&x.length>0;
export function validateMemory(m){if(!m||m.schema!==2||!Array.isArray(m.generations)||!Array.isArray(m.lessons)||!Array.isArray(m.rejectedHypotheses)||!Array.isArray(m.successfulPatterns))throw Error('invalid engineering memory schema');for(const g of m.generations){if(!isStr(g.id)||!isStr(g.at)||!isStr(g.parentSha)||!isStr(g.decision))throw Error('invalid generation record')}for(const l of m.lessons){if(!isStr(l.claim)||!Array.isArray(l.evidence)||!isStr(l.verification)||typeof l.confidence!=='number')throw Error('invalid lesson record')}return m}
export function load(file){if(!fs.existsSync(file))return{schema:2,parent:'a17y.lua',generations:[],lessons:[],rejectedHypotheses:[],successfulPatterns:[],lastDecision:null};let m=JSON.parse(fs.readFileSync(file,'utf8'));if(m.schema===1)m={schema:2,parent:m.parent||'a17y.lua',generations:(m.generations||[]).map(g=>({...g,parentSha:g.parentSha||'unknown',decision:g.decision?.status||g.decision||'unknown'})),lessons:(m.lessons||[]).map(x=>typeof x==='string'?{claim:x,evidence:[],verification:'UNVERIFIED',confidence:0}:x),rejectedHypotheses:m.rejectedHypotheses||[],successfulPatterns:m.successfulPatterns||[],lastDecision:m.lastDecision||null};return validateMemory(m)}
export function save(file,m){validateMemory(m);fs.writeFileSync(file,JSON.stringify(m,null,2)+'\n')}
export function lesson({claim,evidence=[],metrics={},candidateSha='',parentSha='',result='',confidence=0,verification='SIMULATION ONLY'}){return{claim,evidence,metrics,candidateSha,parentSha,result,confidence,verification,at:new Date().toISOString()}}
