#!/usr/bin/env node
import http from'node:http';import{spawn}from'node:child_process';import path from'node:path';
const port=Number(process.env.PORT||17303),helper=path.join(import.meta.dirname,'helper.mjs');
const html=`<!doctype html><meta name=viewport content="width=device-width,initial-scale=1"><title>A17Y MT12 Native Helper</title><style>body{margin:0;background:#07111a;color:#eef7ff;font:15px system-ui}.w{max-width:850px;margin:auto;padding:24px}.c{background:#102131;border:1px solid #31506c;border-radius:18px;padding:18px;margin:12px 0}button,input{padding:12px;border-radius:11px;border:1px solid #41617d;background:#183047;color:white;margin:5px}button{font-weight:800}.p{background:#ffd86d;color:#241600}pre{white-space:pre-wrap;background:#050a0f;padding:14px;border-radius:12px}</style><main class=w><h1>A17Y MT12 Native Helper</h1><div class=c><button onclick="run('detect')">DETECT</button><button onclick="run('sync')">SYNC LOGS</button><button onclick="run('backup')">BACK UP RADIO</button></div><div class=c><h2>EdgeTX UF2 update</h2><input id=fw placeholder="Full path to official MT12 .uf2"><button class=p onclick="run('flash-edgetx '+q(fw.value))">COPY VERIFIED UF2</button></div><div class=c><h2>ExpressLRS</h2><input id=pr placeholder="Full path to MT12 ELRS profile JSON"><button onclick="run('open-elrs '+q(pr.value))">OPEN OFFICIAL CONFIGURATOR</button></div><pre id=o>Ready.</pre></main><script>const o=document.getElementById('o'),q=s=>'"'+s.replaceAll('"','\\"')+'"';async function run(c){o.textContent='Running '+c+'…';const r=await fetch('/run',{method:'POST',body:c});o.textContent=await r.text()}</script>`;
function exec(cmd){return new Promise(resolve=>{const parts=cmd.match(/(?:[^\s"]+|"[^"]*")+/g)?.map(x=>x.replace(/^"|"$/g,''))||[],p=spawn(process.execPath,[helper,...parts],{cwd:path.resolve(import.meta.dirname,'..')});let s='';p.stdout.on('data',d=>s+=d);p.stderr.on('data',d=>s+=d);p.on('close',c=>resolve(`exit ${c}\n${s}`))})}
http.createServer(async(req,res)=>{if(req.method==='GET'){res.setHeader('content-type','text/html');return res.end(html)}if(req.method==='POST'&&req.url==='/run'){let b='';req.on('data',d=>b+=d);req.on('end',async()=>res.end(await exec(b)));return}res.statusCode=404;res.end()}).listen(port,'127.0.0.1',()=>console.log(`A17Y helper: http://127.0.0.1:${port}`));
