#!/usr/bin/env node
import fs from'node:fs';import path from'node:path';import cp from'node:child_process';
const file=process.argv[2];if(!file)throw Error('candidate required');const root=process.cwd(),R=path.join(root,'reports'),base=path.basename(file,'.lua'),pending=JSON.parse(fs.readFileSync(path.join(R,'pending-factory.json'),'utf8')),meta=pending.batch.find(x=>path.basename(x.file,'.lua')===base)||{},parent=meta.parent||pending.lastParent;const changed=meta.functions||meta.changedFunctions||meta.changes?.map(x=>x.function).filter(Boolean)||[];
const report={engine:'function-ablation-v1',candidate:base,parent,changedFunctions:[...new Set(changed)],tests:[],available:false,passed:true};
if(parent&&fs.existsSync(parent)&&report.changedFunctions.length){report.available=true;const candidate=fs.readFileSync(file,'utf8'),p=fs.readFileSync(parent,'utf8');const extract=(s,n)=>{const re=new RegExp(`local function\\s+${n}\\b[\\s\\S]*?\\nend(?:\\n|$)`);return s.match(re)?.[0]};for(const n of report.changedFunctions){const cf=extract(candidate,n),pf=extract(p,n);report.tests.push({function:n,candidateFound:!!cf,parentFound:!!pf,changed:!!cf&&!!pf&&cf!==pf,bytesDelta:(cf?.length||0)-(pf?.length||0)});}report.passed=report.tests.every(x=>x.candidateFound&&x.parentFound&&x.changed)}
fs.writeFileSync(path.join(R,`${base}-ablation.json`),JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2));if(!report.passed)process.exit(2);
