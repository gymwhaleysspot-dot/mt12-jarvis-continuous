#!/usr/bin/env node
import fs from'node:fs';import path from'node:path';import crypto from'node:crypto';
const root=process.cwd(),pp=path.join(root,'reports/pending-factory.json'),rp=path.join(root,'reports/research-latest.md');
if(!fs.existsSync(pp))throw Error('pending-factory.json missing');const token=process.env.GITHUB_TOKEN;if(!token)throw Error('GITHUB_TOKEN missing');
const p=JSON.parse(fs.readFileSync(pp,'utf8')),research=fs.existsSync(rp)?fs.readFileSync(rp,'utf8').slice(0,18000):'';
const model=process.env.FACTORY_MODEL||'openai/gpt-4.1',clean=x=>x.replace(/^```(?:json|lua)?\s*/i,'').replace(/\s*```$/,'').trim(),hash=x=>crypto.createHash('sha256').update(x).digest('hex');
async function ask(messages,max=12000,temp=.2){for(let a=0;a<3;a++){const ctl=new AbortController(),t=setTimeout(()=>ctl.abort(),240000);try{const r=await fetch('https://models.github.ai/inference/chat/completions',{method:'POST',signal:ctl.signal,headers:{Accept:'application/vnd.github+json','Content-Type':'application/json',Authorization:`Bearer ${token}`,'X-GitHub-Api-Version':'2026-03-10'},body:JSON.stringify({model,messages,temperature:temp,max_tokens:max})});if(r.ok){const j=await r.json();return{content:clean(j.choices?.[0]?.message?.content||''),usage:j.usage||null,model:j.model||model}}const e=await r.text();if(a===2)throw Error(`Models ${r.status}: ${e.slice(0,500)}`)}catch(e){if(a===2)throw e;await new Promise(r=>setTimeout(r,2500*(a+1)))}finally{clearTimeout(t)}}}
function segments(src){const lines=src.split('\n'),starts=[];for(let i=0;i<lines.length;i++)if(/^local function\s+[A-Za-z_][A-Za-z0-9_]*\s*\(/.test(lines[i]))starts.push(i);if(!starts.length)throw Error('No top-level functions found');const out=[{name:'PREAMBLE',text:lines.slice(0,starts[0]).join('\n'),fn:[]}];for(let k=0;k<starts.length;k++){const a=starts[k],b=k+1<starts.length?starts[k+1]:lines.length,text=lines.slice(a,b).join('\n'),fn=[...text.matchAll(/local function\s+([A-Za-z_][A-Za-z0-9_]*)\s*\(/g)].map(x=>x[1]);out.push({name:fn[0],text,fn})}return out}
function role(s){const x=s.toLowerCase();if(/lcd|dashboard|drawtext|drawrectangle/.test(x))return'Rally cockpit and observability specialist';if(/tc|abs|slip|gyro|governor|brake/.test(x))return'Predictive vehicle-control specialist';if(/rpm|gps|truth|speed|gear|lag/.test(x))return'Sensor-fusion and state-estimation specialist';if(/neural|learn|trait|mood|confidence|reason/.test(x))return'Online-learning and reasoning specialist';if(/voice|phrase|playfile/.test(x))return'Voice and personality specialist';if(/log|memory|io\.|blackbox|csv/.test(x))return'Embedded memory and logging specialist';return'Embedded Lua efficiency and reliability specialist'}
function packs(segs){const a=[];let cur=[] ,n=0;for(const s of segs){if(s.name==='PREAMBLE')continue;if(cur.length&&n+s.text.length>14000){a.push(cur);cur=[];n=0}cur.push(s);n+=s.text.length}if(cur.length)a.push(cur);return a}
const chosen=[];for(const fam of['monday','tuesday','cloud','html']){const c=p.batch.find(x=>x.family===fam);if(c)chosen.push(c)}p.batch=chosen;p.candidateCount=chosen.length;
for(const c of p.batch){const file=path.join(root,c.file),src=fs.readFileSync(file,'utf8'),segs=segments(src),names=segs.flatMap(x=>x.fn),chief=await ask([{role:'system',content:'You are chief architect of an embedded rally-car AI controller. Return strict JSON only.'},{role:'user',content:`Create a concise architecture plan for the ${c.family} family. The final controller must preserve EdgeTX interfaces, GVars 0-8, aicmem compatibility, fail-open TCT=1024, truth RPM/MPH, TC, ABS, gyro, torque governor, neural learning, voice, logger and a functional rally cockpit. It may redesign everything internally and visually. Functions: ${names.join(', ')}. Research:\n${research.slice(0,9000)}\nReturn {"vision":"...","priorities":[...],"dashboard":"...","safety":[...]}.`}],5000,.25);let plan;try{plan=JSON.parse(chief.content)}catch{plan={vision:chief.content.slice(0,1200),priorities:[],dashboard:'functional rally cockpit',safety:[]}}
 const rebuilt=[segs[0].text],audit=[],usages=[chief.usage];for(const pack of packs(segs)){const old=pack.map(x=>x.text).join('\n'),fns=pack.flatMap(x=>x.fn),r=await ask([{role:'system',content:`You are the ${role(old)}. Return Lua source only, no markdown or explanation.`},{role:'user',content:`Rewrite this complete Lua section with genuinely new, smarter, more efficient and more dynamic implementations. Preserve every function name and call contract. You may reorganize internals inside this section. Immediately after every named local function declaration insert --@regen:NAME. Required markers: ${fns.join(', ')}. Follow this chief plan: ${JSON.stringify(plan).slice(0,6000)}. Preserve MT12 Lua 5.3 compatibility and minimize allocations. SECTION:\n${old}`}],Math.min(24000,Math.max(8000,Math.ceil(old.length*.7))),.22);let out=r.content;for(const n of fns)if(!out.includes(`--@regen:${n}`))throw Error(`${c.family}: specialist omitted ${n}`);out=out.replace(/--@regen:[A-Za-z_][A-Za-z0-9_]*\s*/g,'');if(hash(out.replace(/\s+/g,''))===hash(old.replace(/\s+/g,'')))throw Error(`${c.family}: specialist left section unchanged`);rebuilt.push(out);audit.push({specialist:role(old),functions:fns,beforeBytes:Buffer.byteLength(old),afterBytes:Buffer.byteLength(out)});usages.push(r.usage)}
 const out=rebuilt.join('\n');if(out.length<50000)throw Error(`${c.family}: integrated source incomplete`);if(!/return\s*\{[\s\S]*init\s*=[\s\S]*run\s*=[\s\S]*background\s*=/.test(out))throw Error(`${c.family}: entry table missing`);fs.writeFileSync(file,out);c.changes=[{kind:'architecture',function:'ALL',variant:'multi-agent-clean-sheet',label:'Chief architect plus subsystem specialist regeneration',summary:`${audit.length} specialist passes rebuilt ${names.length} named functions`,from:'single-parent architecture',to:'research-directed multi-agent architecture'},...(c.changes||[])];c.functionChanges=names.length;c.totalNamedFunctions=names.length;c.allFunctionsRewritten=true;c.dynamic=true;c.researchDriven=true;c.aiArchitect=true;c.multiAgent=true;c.aiModel=model;c.architecturePlan=plan;c.specialistAudit=audit;c.aiUsage=usages;c.rewrittenFunctions=names;c.sourceBytes=Buffer.byteLength(out);c.sha256=hash(out)}
p.engine='a15xq-multi-agent-v8';p.mode='chief-specialists-integrator';p.requirements={completeSource:true,allNamedFunctionsRewritten:true,multiAgent:true,researchBeforeBuild:true,functionalDashboard:true,luaDownload:true,luacDownload:true,changelogPerFile:true};fs.writeFileSync(pp,JSON.stringify(p,null,2));console.log(`Multi-agent factory integrated ${p.batch.length} controllers`);
