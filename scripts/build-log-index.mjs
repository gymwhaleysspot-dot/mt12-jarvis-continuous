#!/usr/bin/env node
import fs from'node:fs';import path from'node:path';import crypto from'node:crypto';
const root='tests/replays',files=[];function walk(d){if(!fs.existsSync(d))return;for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(/\.csv$/i.test(e.name)){const b=fs.readFileSync(p);files.push({path:p.replaceAll('\\','/'),bytes:b.length,sha256:crypto.createHash('sha256').update(b).digest('hex')})}}}walk(root);files.sort((a,b)=>a.path.localeCompare(b.path));fs.mkdirSync('public/control-data',{recursive:true});fs.writeFileSync('public/control-data/log-hashes.json',JSON.stringify({schema:1,generatedAt:new Date().toISOString(),count:files.length,files},null,2)+'\n');console.log(`Indexed ${files.length} logs`);
