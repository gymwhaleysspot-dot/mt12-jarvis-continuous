name: MomTo CI and Recovery Guard

on:
  pull_request:
    branches: [main]
    paths:
      - 'momto/**'
      - 'momto-live.html'
      - 'momto-live.json'
      - '.github/workflows/momto-ci.yml'
  push:
    branches: [main]
    paths:
      - 'momto/**'
      - 'momto-live.html'
      - 'momto-live.json'
      - '.github/workflows/momto-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  momto:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 1
      - name: Install MomTo test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest sqlalchemy fastapi typer
      - name: Compile MomTo
        run: python -m compileall -q momto
      - name: Run MomTo tests
        run: python -m pytest momto/tests -q
      - name: Validate public dashboard contract
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          d=json.loads(Path("momto-live.json").read_text())
          assert d["case"]["status"] == "active"
          assert set(d["objectives"]) == {"birth_mother","birth_father"}
          assert len(d["ohio"]["roadmap"]) == 8
          assert len(d["dna"]) == 4
          assert "birth_name" not in d
          assert "advanced" in d and "next_actions" in d
          assert "heartbeat" in d and d["heartbeat"]["privacy_safe"] is True
          assert "persistence" in d and d["persistence"]["private_case_data_persisted"] is False
          assert "answers" in d and isinstance(d["answers"], list)
          assert "activity" in d and "search_ai" in d
          assert d.get("dashboard_version") == "momto-live-v7"
          assert d.get("research", {}).get("total") == 3
          assert d["enhancements"]["total"] == 5000 and d["enhancements"]["public_safe"] is True
          print("public MomTo snapshot is aggregate-only")
          PY
      - name: Validate MomTo live dashboard contracts
        run: |
          python - <<'PY'
          from pathlib import Path
          required=("Birth Mother","Birth Father","Live Activity","Search AI","5000-Enhancement Program","Refresh now","momto-live.json")
          for name in ("momto-live.html","momto/web/momto-live.html"):
              text=Path(name).read_text()
              assert all(x in text for x in required), name
              assert "\\n document.getElementById" not in text, name
              assert "setInterval(refresh,15000)" in text, name
          print("both MomTo dashboards passed live UI contract")
          PY
      - name: Collect failure diagnostics
        if: failure()
        run: |
          mkdir -p artifacts
          python -m pytest momto/tests -q --tb=short > artifacts/momto-test-failure.txt 2>&1 || true
          python -m compileall momto > artifacts/momto-compile.txt 2>&1 || true
      - name: Upload failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: momto-ci-diagnostics-${{ github.sha }}
          path: artifacts/
          retention-days: 14
