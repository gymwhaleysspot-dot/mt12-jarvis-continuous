name: MT12 Master AI Continuous

on:
  workflow_dispatch:
  schedule:
    - cron: '17 * * * *'

permissions:
  contents: write

concurrency:
  group: mt12-master-ai
  cancel-in-progress: false

jobs:
  engineer:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      OPENAI_MODEL: ${{ vars.OPENAI_MODEL || 'gpt-5' }}
      MT12_LIMIT: '88944'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Install Lua 5.3
        run: sudo apt-get update && sudo apt-get install -y lua5.3
      - name: Install cloud AI runtime
        run: npm install
      - name: Generate one candidate
        run: node scripts/master-ai.mjs
      - name: Compile, normalize and gate
        id: gate
        shell: bash
        run: |
          set +e
          CANDIDATE=$(ls generations/candidates/*.lua | sort | tail -1)
          BASE=$(basename "$CANDIDATE" .lua)
          RAW="generations/candidates/${BASE}_raw.luac"
          FINAL="generations/candidates/${BASE}.luac"
          REPORT="reports/${BASE}-gate.txt"
          PASS=1
          {
            echo "candidate=$CANDIDATE"
            echo "started=$(date -u +%FT%TZ)"
            node scripts/static-gate.mjs "$CANDIDATE" || PASS=0
            luac5.3 -p "$CANDIDATE" || PASS=0
            luac5.3 -s -o "$RAW" "$CANDIDATE" || PASS=0
            if [ "$PASS" = 1 ]; then node toolchain/normalize_luac53_mt12.js "$RAW" "$FINAL" || PASS=0; fi
            if [ "$PASS" = 1 ]; then
              BYTES=$(wc -c < "$FINAL" | tr -d ' ')
              echo "normalized_bytes=$BYTES"
              echo "margin=$((MT12_LIMIT-BYTES))"
              [ "$BYTES" -le "$MT12_LIMIT" ] || PASS=0
            fi
            echo "passed=$PASS"
            echo "finished=$(date -u +%FT%TZ)"
          } > "$REPORT" 2>&1
          cat "$REPORT"
          echo "candidate=$CANDIDATE" >> "$GITHUB_OUTPUT"
          echo "base=$BASE" >> "$GITHUB_OUTPUT"
          echo "pass=$PASS" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Promote passing generation
        if: steps.gate.outputs.pass == '1'
        shell: bash
        run: |
          BASE='${{ steps.gate.outputs.base }}'
          cp "generations/candidates/${BASE}.lua" "generations/accepted/${BASE}.lua"
          cp "generations/candidates/${BASE}.luac" "generations/accepted/${BASE}.luac"
          cp "reports/${BASE}-gate.txt" "reports/latest-accepted.txt"
          printf '{"generation":"%s","status":"accepted","updated":"%s","luac":"generations/accepted/%s.luac"}\n' "$BASE" "$(date -u +%FT%TZ)" "$BASE" > reports/status.json
      - name: Record rejected generation
        if: steps.gate.outputs.pass != '1'
        shell: bash
        run: |
          BASE='${{ steps.gate.outputs.base }}'
          printf '{"generation":"%s","status":"rejected","updated":"%s","report":"reports/%s-gate.txt"}\n' "$BASE" "$(date -u +%FT%TZ)" "$BASE" > reports/status.json
      - name: Commit engineering evidence
        shell: bash
        run: |
          git config user.name 'mt12-master-ai'
          git config user.email 'mt12-master-ai@users.noreply.github.com'
          git add generations reports
          git diff --cached --quiet && exit 0
          STATUS=$(node -e "console.log(JSON.parse(require('fs').readFileSync('reports/status.json')).status)")
          git commit -m "Master AI cycle: ${STATUS} $(date -u +%Y-%m-%dT%H:%MZ)"
          git push
      - name: Upload cycle evidence
        uses: actions/upload-artifact@v4
        with:
          name: mt12-master-ai-cycle
          path: |
            generations/candidates/
            generations/accepted/
            reports/
          retention-days: 30
